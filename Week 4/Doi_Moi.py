def cay_da_chat(x):
    return ((a + b) * x) - (a * (x // k)) - (b * (x // m))

def tim_ngay_thoa_man():
    low = 1
    high = int(1e18)
    result = 0

    while low <= high:
        mid = (low + high) // 2
        else:
            low = mid + 1
    
    return result

a, k, b, m, n = map(int, input().split())

print(tim_ngay_thoa_man())